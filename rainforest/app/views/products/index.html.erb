<h1>Products</h1>
<%= link_to "New Product", "/products/new" %><br><br>
<% @products.each do |product| %>
	<p><%= product[:name] %>, <%= product[:description] %>, <%= product[:price_in_cents]%></p>
	<%= link_to "Product Details and Options", "/products/#{product.id}" %><br><br>
<% end %>
